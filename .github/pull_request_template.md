## Goal

<!-- One or two sentences on why this change exists. -->

## Summary

<!-- What changed. -->

## Checks

- [ ] `uv run pytest` passes
- [ ] `uv run mypy` is clean
- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass
- [ ] Any new promise in the README has a test that proves it
